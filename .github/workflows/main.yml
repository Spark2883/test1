on:
    push:
      branches:
        - master
  
  jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: 'Test'
          run: |
            echo abdo
            pwd
        - name: 'Testing'
          run: echo abdo
  